# Copy to .env.local (do NOT commit .env.local).
# Sanity project configuration.
NEXT_PUBLIC_SANITY_PROJECT_ID=4tij5rov
NEXT_PUBLIC_SANITY_DATASET=production

# Editor-role token (required for `npm run sanity:sync` and `npm run sanity:seed`).
# Buat di https://www.sanity.io/manage -> API -> Tokens (role: Editor).
SANITY_API_TOKEN=
